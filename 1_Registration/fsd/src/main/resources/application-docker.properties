# Docker environment configuration for Registration Service
spring.application.name=registration-service

# Database configuration for Docker
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://${DB_HOST:registration-db}:${DB_PORT:5432}/${DB_NAME:registrationdb}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:root}
spring.jpa.hibernate.ddl-auto=update

# Service URLs for Docker environment
services.participation.url=${PARTICIPATION_SERVICE_URL:http://participation-service:8080}

# Server configuration
server.port=8080

# Mail configuration (same as default)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=vigomi024@gmail.com
spring.mail.password=fzao caxk rqvg lnyx
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.transport.protocol=smtp
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.connectiontimeout=5000